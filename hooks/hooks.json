{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__.*__write_note",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before saving this note: if you haven't already invoked the placement skill for this specific write (or the directory was set by explicit user instruction or basic-memory.md config), invoke it now. If placement is already settled for this write, proceed without re-invoking."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "mcp__.*__write_note",
        "hooks": [
          {
            "type": "command",
            "command": "echo '✓ Note saved to Basic Memory'"
          }
        ]
      }
    ]
  }
}
