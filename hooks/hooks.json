{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__basic-memory__write_note",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before saving this note to Basic Memory, validate it using the validate-memo skill if the hooks server is running at http://localhost:4665. Check the content against the project's format configuration to catch and fix any formatting issues. If validation fails with errors, fix the content before proceeding."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "mcp__basic-memory__write_note",
        "hooks": [
          {
            "type": "command",
            "command": "echo '✓ Note saved to Basic Memory'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If this conversation contained valuable insights, decisions, or learnings that should be preserved, suggest using `/remember [title]` to capture them in Basic Memory. Only suggest this if there's genuinely valuable content worth preserving - don't suggest for trivial interactions."
          }
        ]
      }
    ]
  }
}
